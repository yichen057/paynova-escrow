# Fixed test-only key (test environment only, no security value) — the main config has no default key and fails fast
paynova.jwt.secret=dGVzdC1vbmx5LXNlY3JldC1rZXktMzItYnl0ZXMtbG9uZyEhIQ==
paynova.jwt.expiry-minutes=60
# CI runs deterministic tests only: disable Outbox background scheduling; OutboxFlowIT drives the Worker/Reaper manually
paynova.outbox.scheduling-enabled=false
