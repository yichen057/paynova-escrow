<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging profiles (design doc §11 — SIEM-ready, not SIEM-dependent):
  - default: human-readable console logs for local development
  - "json" Spring profile: one JSON object per line (LogstashEncoder, MDC included:
    correlation_id + source_ip), ready for Splunk/ELK ingestion.
    Activate with: SPRING_PROFILES_ACTIVE=json
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="json">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!json">
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
